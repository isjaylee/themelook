<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">
    <title>Themelook - Find themes and templates easily.</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/bootstrap.css") %>">
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/style.css") %>">
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/font-awesome.css") %>">
    <script src="<%= static_path(@conn, "/js/lodash.js") %>"></script>
    <script src="<%= static_path(@conn, "/js/angular.js") %>"></script>
    <script src="<%= static_path(@conn, "/js/angular/libraries/ui-bootstrap.js") %>"></script>
    <script src="<%= static_path(@conn, "/js/jquery.js") %>"></script>
    <script src="<%= static_path(@conn, "/js/bootstrap.js") %>"></script>
    <script src="<%= static_path(@conn, "/js/accounting.js") %>"></script>

    <!-- Google Analytics -->
    <script>
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

      ga('create', 'UA-16135295-16', 'auto');
      ga('send', 'pageview');

    </script>
  </head>
  <body class="gird-2">
    <%= render Themelook.LayoutView, "navigation.html", conn: @conn, current_user: @current_user, home_categories: @home_categories, frameworks: @frameworks %>
    <%= if !assigns[:disable_search_form], do: render Themelook.LayoutView, "search_form.html", conn: @conn, current_user: @current_user, home_categories: @home_categories, frameworks: @frameworks %>
    <div class="main-content-section">
      <div class="container">
        <div class="row">
          <%= if !assigns[:disable_sidebar], do: render Themelook.LayoutView, "sidebar.html", conn: @conn, home_categories: @home_categories, frameworks: @frameworks %>
          <%= render @view_module, @view_template, assigns %>
        </div>
      </div>
    </div>
    <%= render Themelook.LayoutView, "footer.html", conn: @conn %>
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
